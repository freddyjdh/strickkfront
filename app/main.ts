
import { gsap } from "gsap";
    
import { ScrollTrigger } from "gsap/ScrollTrigger";


gsap.registerPlugin(ScrollTrigger);

if(typeof document !== 'undefined'){

const mobile = document.querySelector('.mobilemenu') as HTMLDivElement | null;


}